const assert = require('assert')
var expect = require('chai').expect;
var should = require('chai').should();

import { cleanIban, checkInvalidChars, checkStartOfIBAN, checkCountryCode } from '../src/ibanValidator'

describe("When preparing to calculate the checksum", ()=> {

  it.skip("should rearrange the IBAN ready for checking", () => {
  })

  it.skip("should replace letters with values", () => {
  })

})

describe("When calculating the checksum", () => {

  it.skip("should calculate the checksum correctly", () => {
  })

})
